# Shell scripts must keep LF endings — CRLF breaks the shebang inside Linux
# containers ("no such file or directory"). Enforce it regardless of core.autocrlf.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
